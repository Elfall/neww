import React from 'react';

export const Slider: React.FC<{ defaultValue: number[]; max: number; step: number; className?: string }> = ({ defaultValue, max, step, className }) => (
  <div className={className}>
    {/* Implementa qui la logica dello slider */}
  </div>
);